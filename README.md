# NLC Weekly Dashboard

A small internal web dashboard for an accounting firm. Drop in a CSV or Excel
export from Karbon and see:

- **How many pending documents** each person has to send to clients
- **Who has the most on their plate** (workload leaderboard + charts)
- **How long** each document has been open, and **what's overdue**
- A **Weekly Review** — the most overdue work firm-wide, plus a page per staff
  member — viewable on screen, printable as a PDF, or exported to Excel

It's a Flask app that runs on one Windows machine; everyone else opens it in a
browser.

> ### ⚠️ There is no sign-in
> The dashboard has no accounts and no passwords. **Anyone who can reach the port
> sees everything** and can import or delete data. That is fine on a private LAN
> and nowhere else — never expose it to the internet. See
> [SECURITY.md](SECURITY.md) before deploying it.

---

## Quick start

You need **Python 3.11+**. Then:

```
git clone https://github.com/NLC-Team/NLC-Weekly-Dashboard.git
cd NLC-Weekly-Dashboard
```

**On Windows**, double-click **`Run Dashboard.bat`**. It installs the
dependencies on first run, checks the app starts, and opens your browser. If
anything goes wrong the window stays open and tells you what.

**On any platform**, or if you'd rather see the server output:

```bash
pip install -r requirements.txt
python src/webapp.py
```

Either way the dashboard is at **<http://127.0.0.1:5000>**.

### You'll land on an empty dashboard — that's expected

The database is not part of the repo (it holds client data, and it's per-user), so
a fresh clone starts with nothing in it. To see the dashboard working immediately,
import the synthetic sample file that ships with it:

1. Go to **Import**.
2. Choose **`sample_data/karbon_doc_sample.csv`** and click *Upload & continue*.
3. Confirm the detected columns, then click **Run import**.

Every page now has data in it. `sample_data/` is entirely made-up — invented
clients and staff, generated by the scripts beside it — so it's safe to play with.
When you're ready, import your own Karbon export the same way.

### Where your data lives

| What | Where |
|------|-------|
| Database + settings | `%LOCALAPPDATA%\KarbonPendingDashboard\` (Windows) |
| Log file | `…\KarbonPendingDashboard\app.log` |
| Database path override | the `db_path` key in `app_config.json`, in that folder |

Keep the database on a local disk. SQLite corrupts if several people open the
same file over a Windows file share — copy it to a share for backup, but don't
run the app against one.

---

## Day-to-day use

- **Re-import regularly** (weekly works well). The app keeps history: an item
  keeps its original "first seen" date, so *days open* keeps growing across
  imports even if your export has no date column. Anything that stops appearing
  is treated as sent or completed.
- **Overdue threshold** is adjustable in **Settings** (default: 14 days).
- Your **column mapping is remembered**, so later imports are one click.
- **Pin it as an app** (optional): the dashboard is an installable web app. In
  Chrome or Edge, use the *Install* icon in the address bar. On machines other
  than the host this needs HTTPS — see [IT help.md](IT%20help.md).

### What the columns mean

Karbon exports can use any column names, so the import screen maps your columns
onto these fields and pre-guesses them for you:

| Field | Required | Meaning |
|-------|:---:|---------|
| Assignee / staff member | yes | Who owns the work |
| Client | yes | The client it's for |
| Document / work title | yes | Name of the statement or work |
| Status | yes | Decides what counts as "pending to send" |
| Client owner / partner | no | Used to scope the Weekly Review |
| Start / created date | no | Used to compute days open |
| Due date | no | Used to decide what's overdue |
| Completed date (per document) | no | Drives "completed this week" |
| Karbon work ID | no | Stable key, so items are tracked across imports |
| Return / project | no | Groups documents into one return |
| Return type | no | 1040, 1120S, Bookkeeping, … |

With no date column the app falls back to how long each item keeps reappearing in
your imports.

---

## Firm-specific settings — read this if your numbers must match someone else's

Five rules are keyed on real names, so they are kept out of this repo; publishing
them would mean publishing employees and clients:

- one assignee whose property line-items are hidden everywhere,
- internal/test clients excluded from every page and export,
- former staff whose work still counts but shows as "Unassigned",
- the partner whose clients the Weekly Review covers,
- how that scope is worded on screen and in the PDF.

They live in `src/local_config.py`, which is **git-ignored** — so a fresh clone
does not have it, and never commit it.

> ### ⚠️ A clone will not show the same figures as an existing installation
> Without `local_config.py` the dashboard runs perfectly well but applies **none**
> of those rules. On the same database that means: **more rows**, a **higher
> overdue count**, former staff listed under their own names instead of
> "Unassigned", excluded internal clients back in the totals, and a Weekly Review
> covering **every** client rather than one partner's book.
>
> Nothing is broken — the two copies are just configured differently. But if you
> are comparing numbers with a colleague, this is almost certainly why they differ.

**To match an existing installation:** get that machine's `src/local_config.py`
and drop it into your `src/` folder, then restart. Send it over a normal internal
channel — it is ordinary configuration, not a secret, but it does name people, so
don't post it anywhere public.

> This file is necessary but **not sufficient** for two machines to agree. The
> database matters too — see
> [Making a second machine show the same figures](#making-a-second-machine-show-the-same-figures).

**Starting fresh instead:** copy the documented template and fill it in.

```bash
cp src/local_config.example.py src/local_config.py
```

Either way, **Settings** tells you which state you're in: a "Firm-specific rules"
card at the top of the page shows *active* with a summary of what's applied, or
*not configured* with an explanation. The startup log
(`%LOCALAPPDATA%\KarbonPendingDashboard\app.log`) says the same thing on every
launch.

---

## Making a second machine show the same figures

Cloning the repo gets you the same *application*. It does **not** get you the same
*dashboard*, because two of the three things that produce the numbers are
deliberately not in version control.

| # | What | Why a clone doesn't have it |
|---|------|------------------------------|
| 1 | The code | ✅ you have it — that's this repo |
| 2 | `src/local_config.py` | Git-ignored: it names real staff and clients |
| 3 | `dashboard.db` | Never committed: it holds client data, and it's per-user |

**Item 3 is the one people underestimate.** The database is not just a cache of
the last import — it accumulates history across imports, and some figures can
only be derived from that history. In particular a **handoff** (Karbon moving a
document to a different assignee) is only detectable by comparing two consecutive
imports, so a database that has seen one import has none.

Measured on a real database with 34 imports behind it, against the same items
imported just once:

| | With the accumulated history | After a single import |
|---|---|---|
| Rows visible | 5,394 | 5,394 (same) |
| Returns / clients | 1,595 | 1,595 (same) |
| **Overdue** | **1,124** | **1,363** |
| **Weekly Review overdue** | **864** | **1,095** |
| **Completed this week** | **51** | **0** |
| Top-10, per-staff counts | — | all shift |

The overdue count inflates because a handed-off document has left its previous
assignee's plate; with no handoff history those rows still count as that person's
open, overdue work. So a fresh import doesn't just lose a nice-to-have — it
reports a materially worse backlog than reality.

### So, which do you actually want?

**To read the firm's real numbers → use the one server, don't run your own copy.**
Open `http://<host>:5000` in a browser. That is what the dashboard is for: one
machine holds the database, everyone else views it. Two copies of the app with two
databases will always drift, and SQLite corrupts if two machines open one file
over a network share — so "just point them both at the same file" is not a fix.

**To develop, test or demo → clone and import `sample_data/`.** You get a fully
working dashboard in a minute, with invented clients, and nothing you do can
affect the real data.

**To genuinely replicate an installation** (rebuilding a host, moving machines):
copy all three — the repo, that machine's `src/local_config.py`, and its
`dashboard.db` from `%LOCALAPPDATA%\KarbonPendingDashboard\`. Copy the `.db` file
while the app is stopped, and remember it contains client data, so move it the way
you'd move any client file.

---

## Developer notes

### Layout

```
src/
  webapp.py          The app: Flask routes, request gate, startup
  config.py          Paths, logical fields, defaults, firm-rule plumbing
  local_config.py    Firm-specific names (git-ignored; see .example.py)
  review_pdf.py      Weekly Review -> PDF
  review_xlsx.py     Weekly Review -> Excel
  data/
    importer.py      CSV/Excel load, column mapping, status filtering
    store.py         SQLite: settings, mappings, item history, audit trail
    analytics.py      Pure functions: age, overdue, per-staff aggregates
    review.py        Weekly Review scope + assembly
    service.py       Glue: import pipeline + dashboard data
  templates/         Jinja templates (base.html is the app shell)
  static/            app.css, icons, service worker, manifest
  main.py, ui/       LEGACY desktop (Tkinter) app — see below
sample_data/         Synthetic Karbon-like CSVs + their generators
tests/               pytest suite
setup/               Scripts to host it for a team (firewall + always-on)
build.spec           PyInstaller spec for the legacy desktop app
```

### Commands

```bash
pip install -r requirements.txt
python src/webapp.py             # run the dashboard
python -m pytest tests -q        # run the tests
python sample_data/generate_sample.py       # regenerate sample CSVs
python sample_data/generate_doc_sample.py
```

`NLC_BIND_HOST` controls what the server listens on. It defaults to `0.0.0.0`
(reachable from the LAN, which is what team hosting needs); set it to
`127.0.0.1` to keep the dashboard to your own machine.

Templates auto-reload, so editing a `.html` file just needs a browser refresh.
Editing a `.py` file needs a server restart.

### On the Windows host

This project's usual host has no system Python (MSI installs are blocked by
policy), so it uses a portable **WinPython** at
`%LOCALAPPDATA%\WP\WPy64-313130\python\python.exe`. `Run Dashboard.bat` prefers
it and falls back to whatever Python is on `PATH`. To recreate it, run a
WinPython "dot" self-extractor from
[github.com/winpython/winpython](https://github.com/winpython/winpython/releases)
— it just extracts files, no installer and no admin rights.

### Hosting it for the team

See [setup/README.md](setup/README.md) (firewall + always-on service) and
[IT help.md](IT%20help.md) (the brief to hand to IT).

### Design notes

- **Age** prefers the real date from the export; otherwise it uses how long the
  item has been tracked (`first_seen`). See `data/analytics.py::item_age_days`.
- **Item identity** uses the Karbon work ID when mapped, else a hash of
  client + title + assignee, so the same item matches across weekly imports. A
  reassignment therefore creates a new row, which is why handoffs are detected
  explicitly (`store.detect_handoffs`).
- **Completed** is counted **once per client** everywhere — except the Weekly
  Review's "completed this week", which is deliberately per-document.
- Analytics are pure functions with no DB or UI, which is why they're unit-tested
  directly.
- The Content-Security-Policy allows no inline script. Event handlers must be
  registered with `addEventListener` (see the delegated `data-*` handlers at the
  bottom of `base.html`) — an `onclick="…"` attribute in a template is silently
  dead code.

### The legacy desktop app

`src/main.py` and `src/ui/` are an earlier **Tkinter desktop** version, kept only
for reference; `build.spec` packages it. It shares `config.py` and `data/` with
the web app but has none of the newer features (Weekly Review, handoffs,
document-level completion). **The web app is the real one** — if you're not sure
which you want, you want `src/webapp.py`.
