# Runtime dependencies — all four are required to start the dashboard.
flask          # the web app itself
waitress       # production WSGI server the app serves through
pandas         # CSV / Excel parsing for the import wizard
matplotlib     # renders the overview charts as PNGs
openpyxl       # read .xlsx / .xls Excel imports, write the review workbook

# Build-only (only needed to rebuild the legacy desktop .exe from build.spec)
pyinstaller

# Test-only
pytest
